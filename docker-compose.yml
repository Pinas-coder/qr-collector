services:
  server:
    build: ./server
    ports:
      - "4000:4000"
    volumes:
      - ./server/data.sqlite:/app/data.sqlite
    restart: unless-stopped

  client:
    build: ./client
    ports:
      - "3000:3000"
    depends_on:
      - server
    restart: unless-stopped
